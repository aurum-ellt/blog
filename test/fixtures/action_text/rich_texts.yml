<% text = Faker::Lorem.paragraphs(number: 20).join(" ") %>

one:
  record: one (Post)
  name: body
  body: <%= text %>

two:
  record: two (Post)
  name: body
  body: <%= text %>

three:
  record: three (Post)
  name: body
  body: <%= text %>

four:
  record: four (Post)
  name: body
  body: <%= text %>
